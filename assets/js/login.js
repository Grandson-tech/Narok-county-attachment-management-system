import { supabase } from './supabase.js';
const form=document.querySelector('#login-form'),alertBox=document.querySelector('#login-alert'),button=document.querySelector('#login-submit');const flash=(t,b=false)=>{alertBox.className=`mt-5 rounded-md border px-4 py-3 text-sm ${b?'border-red-200 bg-red-50 text-red-800':'border-emerald-200 bg-emerald-50 text-emerald-800'}`;alertBox.textContent=t};form.addEventListener('submit',async e=>{e.preventDefault();const values=Object.fromEntries(new FormData(form));button.disabled=true;button.textContent='Signing in…';try{const {data,error}=await supabase.auth.signInWithPassword({email:values.email.trim(),password:values.password});if(error)throw error;const {data:profile}=await supabase.from('profiles').select('role').eq('id',data.user.id).maybeSingle();location.assign(['hr_admin','super_admin'].includes(profile?.role)?'pages/admin-dashboard.html':'pages/dashboard.html')}catch(error){flash(error.message==='Failed to fetch'?'Unable to reach Supabase. Check your connection and configuration.':error.message,true)}finally{button.disabled=false;button.textContent='Sign in'}});
